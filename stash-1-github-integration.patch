diff --git a/.env.example b/.env.example
index da9e067..831066a 100644
--- a/.env.example
+++ b/.env.example
@@ -1,23 +1,88 @@
-# GitHub OAuth Configuration
-# Get these from https://github.com/settings/developers
-# Create a new OAuth App and use these credentials
-NEXT_PUBLIC_GITHUB_CLIENT_ID=your_github_client_id_here
-GITHUB_CLIENT_SECRET=your_github_client_secret_here
-
-# Your application's URL (used for OAuth callbacks)
-NEXT_PUBLIC_URL=http://localhost:3000
-
-# Clerk Authentication (if not already configured)
-NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=
-CLERK_SECRET_KEY=
-
-# Database
-DATABASE_URL=
-
-# Other integrations (Discord, Slack, Notion, etc.)
-DISCORD_CLIENT_ID=
-DISCORD_CLIENT_SECRET=
-SLACK_CLIENT_ID=
-SLACK_CLIENT_SECRET=
-NOTION_CLIENT_ID=
-NOTION_CLIENT_SECRET=
+# =============================================================================
+# CLERK AUTHENTICATION
+# =============================================================================
+NEXT_PUBLIC_CLERK_SIGN_IN_URL=/sign-in
+NEXT_PUBLIC_CLERK_SIGN_UP_URL=/sign-up
+NEXT_PUBLIC_CLERK_AFTER_SIGN_IN_URL=/dashboard
+NEXT_PUBLIC_CLERK_AFTER_SIGN_UP_URL=/dashboard
+
+NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=your_clerk_publishable_key
+CLERK_SECRET_KEY=your_clerk_secret_key
+
+# =============================================================================
+# DATABASE (Neon PostgreSQL)
+# =============================================================================
+# DATABASE_URL - Uses connection pooling (PgBouncer) for queries
+# connection_limit=1 is optimal for serverless/dev (prevents pool exhaustion)
+DATABASE_URL="postgresql://user:password@host-pooler.region.neon.tech/dbname?sslmode=require&pgbouncer=true&connection_limit=1&pool_timeout=10&connect_timeout=10"
+
+# DIRECT_URL - Direct connection for migrations (bypasses pooler)
+DIRECT_URL="postgresql://user:password@host.region.neon.tech/dbname?sslmode=require"
+
+# =============================================================================
+# APPLICATION URLs
+# =============================================================================
+NEXT_PUBLIC_URL=https://localhost:3000
+NEXT_PUBLIC_DOMAIN=localhost:3000
+NEXT_PUBLIC_SCHEME=https://
+
+# =============================================================================
+# GITHUB INTEGRATION (OAuth)
+# Get credentials from: https://github.com/settings/developers
+# Callback URL: https://localhost:3000/api/auth/callback/github
+# =============================================================================
+NEXT_PUBLIC_GITHUB_CLIENT_ID=your_github_client_id
+GITHUB_CLIENT_SECRET=your_github_client_secret
+
+# =============================================================================
+# GOOGLE DRIVE INTEGRATION
+# =============================================================================
+NEXT_PUBLIC_GOOGLE_SCOPES=https://www.googleapis.com/auth/drive
+NEXT_PUBLIC_OAUTH2_ENDPOINT=https://accounts.google.com/o/oauth2/v2/auth
+GOOGLE_CLIENT_ID=your_google_client_id
+GOOGLE_CLIENT_SECRET=your_google_client_secret
+OAUTH2_REDIRECT_URI=your_clerk_oauth_callback_url
+
+# =============================================================================
+# DISCORD INTEGRATION
+# =============================================================================
+DISCORD_CLIENT_ID=your_discord_client_id
+DISCORD_CLIENT_SECRET=your_discord_client_secret
+DISCORD_TOKEN=
+DISCORD_PUBLIC_KEY=your_discord_public_key
+NEXT_PUBLIC_DISCORD_REDIRECT=https://discord.com/oauth2/authorize?client_id=YOUR_CLIENT_ID&response_type=code&redirect_uri=https%3A%2F%2Flocalhost%3A3000%2Fapi%2Fauth%2Fcallback%2Fdiscord&scope=webhook.incoming
+
+# =============================================================================
+# NOTION INTEGRATION
+# =============================================================================
+NOTION_API_SECRET=your_notion_api_secret
+NOTION_CLIENT_ID=your_notion_client_id
+NOTION_REDIRECT_URI=https://localhost:3000/api/auth/callback/notion
+NEXT_PUBLIC_NOTION_AUTH_URL=https://api.notion.com/v1/oauth/authorize?client_id=YOUR_CLIENT_ID&response_type=code&owner=user&redirect_uri=https%3A%2F%2Flocalhost%3A3000%2Fapi%2Fauth%2Fcallback%2Fnotion
+
+# =============================================================================
+# SLACK INTEGRATION
+# =============================================================================
+SLACK_SIGNING_SECRET=your_slack_signing_secret
+SLACK_BOT_TOKEN=your_slack_bot_token
+SLACK_APP_TOKEN=your_slack_app_token
+SLACK_CLIENT_ID=your_slack_client_id
+SLACK_CLIENT_SECRET=your_slack_client_secret
+SLACK_REDIRECT_URI=https://localhost:3000/api/auth/callback/slack
+NEXT_PUBLIC_SLACK_REDIRECT=https://slack.com/oauth/v2/authorize?client_id=YOUR_CLIENT_ID&scope=chat:write,channels:read,groups:read,mpim:read,im:read&user_scope=chat:write,channels:read,groups:read,mpim:read,im:read&redirect_uri=https%3A%2F%2Flocalhost%3A3000%2Fapi%2Fauth%2Fcallback%2Fslack
+
+# =============================================================================
+# UPLOADCARE (File uploads)
+# =============================================================================
+NEXT_PUBLIC_UPLOAD_CARE_CSS_SRC=https://cdn.jsdelivr.net/npm/@uploadcare/blocks@
+NEXT_PUBLIC_UPLOAD_CARE_SRC_PACKAGE=/web/lr-file-uploader-regular.min.css
+
+# =============================================================================
+# NGROK (For local webhook testing)
+# =============================================================================
+NGROK_URI=https://your-subdomain.ngrok-free.app
+
+# =============================================================================
+# CRON JOB SECURITY
+# =============================================================================
+CRON_JOB_KEY=your_random_secure_key_here
diff --git a/prisma/schema.prisma b/prisma/schema.prisma
index 968d1b1..7518c01 100644
--- a/prisma/schema.prisma
+++ b/prisma/schema.prisma
@@ -1,130 +1,120 @@
-// This is your Prisma schema file,
-// learn more about it in the docs: https://pris.ly/d/prisma-schema
-
 generator client {
   provider = "prisma-client-js"
 }
 
 datasource db {
-  provider = "postgresql"
-  url      = env("DATABASE_URL")
+  provider  = "postgresql"
+  url       = env("DATABASE_URL")
+  directUrl = env("DIRECT_URL")
 }
 
-model User {
-  id Int @id @default(autoincrement())
-
-  clerkId      String  @unique
-  name         String?
-  email        String  @unique
-  profileImage String?
-  tier         String? @default("Free")
-  credits      String? @default("10")
-
-  createdAt        DateTime @default(now())
-  updatedAt        DateTime @updatedAt
-  localGoogleId    String?  @unique
-  googleResourceId String?  @unique
-
-  LocalGoogleCredential LocalGoogleCredential?
-  DiscordWebhook        DiscordWebhook[]
-  Notion                Notion[]
-  Slack                 Slack[]
-  GitHub                GitHub[]
-  connections           Connections[]
-  workflows             Workflows[]
-}
-
-model LocalGoogleCredential {
-  id          String @id @default(uuid())
-  accessToken String @unique
-
-  folderId   String?
-  pageToken  String?
-  channelId  String  @unique @default(uuid())
-  subscribed Boolean @default(false)
-
-  createdAt DateTime @default(now())
-  updatedAt DateTime @updatedAt
+model Connections {
+  id               String          @id @default(uuid())
+  type             String
+  discordWebhookId String?
+  notionId         String?
+  userId           String?
+  slackId          String?
+  gitHubId         String?
+  DiscordWebhook   DiscordWebhook? @relation(fields: [discordWebhookId], references: [id])
+  GitHub           GitHub?         @relation(fields: [gitHubId], references: [id])
+  Notion           Notion?         @relation(fields: [notionId], references: [id])
+  Slack            Slack?          @relation(fields: [slackId], references: [id])
+  User             User?           @relation(fields: [userId], references: [clerkId])
 
-  userId Int  @unique
-  user   User @relation(fields: [userId], references: [id])
+  @@unique([userId, type])
 }
 
 model DiscordWebhook {
-  id          String        @id @default(uuid())
+  id          String        @id
   webhookId   String        @unique
   url         String        @unique
   name        String
   guildName   String
   guildId     String
   channelId   String        @unique
-  user        User          @relation(fields: [userId], references: [clerkId])
   userId      String
-  connections Connections[]
+  Connections Connections[]
+  User        User          @relation(fields: [userId], references: [clerkId])
 }
 
-model Slack {
-  id String @id @default(uuid())
-
-  appId            String
-  authedUserId     String
-  authedUserToken  String @unique
-  slackAccessToken String @unique
-  botUserId        String
-  teamId           String
-  teamName         String
+model GitHub {
+  id             String        @id @default(uuid())
+  accessToken    String        @unique
+  refreshToken   String?
+  login          String
+  avatarUrl      String?
+  name           String?
+  repositories   String[]
+  webhookId      String?
+  webhookSecret  String?
+  selectedRepo   String?
+  selectedEvents String[]
+  userId         String
+  createdAt      DateTime      @default(now())
+  updatedAt      DateTime      @updatedAt
+  Connections    Connections[]
+  User           User          @relation(fields: [userId], references: [clerkId])
+}
 
-  User        User          @relation(fields: [userId], references: [clerkId])
-  userId      String
-  connections Connections[]
+model LocalGoogleCredential {
+  id          String   @id
+  accessToken String   @unique
+  folderId    String?
+  pageToken   String?
+  channelId   String   @unique
+  subscribed  Boolean  @default(false)
+  createdAt   DateTime @default(now())
+  updatedAt   DateTime
+  userId      Int      @unique
+  User        User     @relation(fields: [userId], references: [id])
 }
 
 model Notion {
-  id            String        @id @default(uuid())
+  id            String        @id
   accessToken   String        @unique
   workspaceId   String        @unique
   databaseId    String        @unique
   workspaceName String
   workspaceIcon String
-  User          User          @relation(fields: [userId], references: [clerkId])
   userId        String
-  connections   Connections[]
+  Connections   Connections[]
+  User          User          @relation(fields: [userId], references: [clerkId])
 }
 
-model GitHub {
-  id              String        @id @default(uuid())
-  accessToken     String        @unique
-  refreshToken    String?
-  login           String
-  avatarUrl       String?
-  name            String?
-  repositories    String[] // Store repo IDs or full names
-  webhookId       String?
-  webhookSecret   String?
-  selectedRepo    String? // Currently selected repo for trigger
-  selectedEvents  String[] // Events like push, pull_request, etc.
-  User            User          @relation(fields: [userId], references: [clerkId])
-  userId          String
-  connections     Connections[]
-  createdAt       DateTime      @default(now())
-  updatedAt       DateTime      @updatedAt
+model Slack {
+  id               String        @id
+  appId            String
+  authedUserId     String
+  authedUserToken  String        @unique
+  slackAccessToken String        @unique
+  botUserId        String
+  teamId           String
+  teamName         String
+  userId           String
+  Connections      Connections[]
+  User             User          @relation(fields: [userId], references: [clerkId])
 }
 
-model Connections {
-  id               String          @id @default(uuid())
-  type             String
-  DiscordWebhook   DiscordWebhook? @relation(fields: [discordWebhookId], references: [id])
-  discordWebhookId String?
-  Notion           Notion?         @relation(fields: [notionId], references: [id])
-  notionId         String?
-  User             User?           @relation(fields: [userId], references: [clerkId])
-  userId           String?
-  Slack            Slack?          @relation(fields: [slackId], references: [id])
-  slackId          String?
-  GitHub           GitHub?         @relation(fields: [gitHubId], references: [id])
-  gitHubId         String?
-
-  @@unique([userId, type])
+model User {
+  id                    Int                    @id @default(autoincrement())
+  clerkId               String                 @unique
+  name                  String?
+  email                 String                 @unique
+  profileImage          String?
+  tier                  String?                @default("Free")
+  credits               String?                @default("10")
+  createdAt             DateTime               @default(now())
+  updatedAt             DateTime               @updatedAt
+  localGoogleId         String?                @unique
+  googleResourceId      String?                @unique
+  Connections           Connections[]
+  DiscordWebhook        DiscordWebhook[]
+  GitHub                GitHub[]
+  LocalGoogleCredential LocalGoogleCredential?
+  Notion                Notion[]
+  Slack                 Slack[]
+  Workflows             Workflows[]
 }
 
 model Workflows {
@@ -139,15 +129,15 @@ model Workflows {
   slackAccessToken  String?
   notionAccessToken String?
   notionDbId        String?
-  githubRepo        String? // Selected GitHub repo for trigger
-  githubEvents      String[] // Events that trigger workflow
-  githubAccessToken String? // GitHub access token for this workflow
   flowPath          String?
   cronPath          String?
   publish           Boolean? @default(false)
   description       String
+  userId            String
   createdAt         DateTime @default(now())
   updatedAt         DateTime @updatedAt
+  githubAccessToken String?
+  githubEvents      String[]
+  githubRepo        String?
   User              User     @relation(fields: [userId], references: [clerkId])
-  userId            String
 }
diff --git a/public/github.png b/public/github.png
index 525b051..e28a837 100644
Binary files a/public/github.png and b/public/github.png differ
diff --git a/src/app/(main)/(pages)/connections/_actions/get-user.tsx b/src/app/(main)/(pages)/connections/_actions/get-user.tsx
index a0e0697..7e3e906 100644
--- a/src/app/(main)/(pages)/connections/_actions/get-user.tsx
+++ b/src/app/(main)/(pages)/connections/_actions/get-user.tsx
@@ -44,7 +44,7 @@ export const getUserData = async (id?: string) => {
         name,
         profileImage: clerkUser?.imageUrl ?? undefined,
       },
-      include: { connections: true },
+      include: { Connections: true },
     })
 
     console.log('User upserted successfully:', user_info.clerkId)
diff --git a/src/app/(main)/(pages)/connections/_actions/github-connection.tsx b/src/app/(main)/(pages)/connections/_actions/github-connection.tsx
index 0d860af..675abcd 100644
--- a/src/app/(main)/(pages)/connections/_actions/github-connection.tsx
+++ b/src/app/(main)/(pages)/connections/_actions/github-connection.tsx
@@ -2,6 +2,68 @@
 
 import { db } from '@/lib/db'
 import axios from 'axios'
+import { currentUser } from '@clerk/nextjs/server'
+
+export const getCurrentUserGitHubConnection = async () => {
+  try {
+    const user = await currentUser()
+    if (!user) return null
+    
+    const connection = await db.gitHub.findFirst({
+      where: { userId: user.id },
+    })
+    return connection
+  } catch (error) {
+    console.error('Error getting current user GitHub connection:', error)
+    return null
+  }
+}
+
+export const getLatestCommitFromRepo = async (repo: string) => {
+  try {
+    const user = await currentUser()
+    if (!user) return null
+    
+    const connection = await db.gitHub.findFirst({
+      where: { userId: user.id },
+    })
+    
+    if (!connection) return null
+
+    // Fetch latest commit from GitHub API
+    const response = await axios.get(
+      `https://api.github.com/repos/${repo}/commits`,
+      {
+        headers: {
+          Authorization: `Bearer ${connection.accessToken}`,
+          Accept: 'application/vnd.github.v3+json',
+        },
+        params: {
+          per_page: 1,
+        },
+      }
+    )
+
+    if (response.data && response.data.length > 0) {
+      const commit = response.data[0]
+      return {
+        sha: commit.sha,
+        message: commit.commit.message,
+        author: commit.commit.author.name,
+        authorEmail: commit.commit.author.email,
+        timestamp: commit.commit.author.date,
+        repository: repo,
+        url: commit.html_url,
+        branch: 'main', // Default branch, can be enhanced
+      }
+    }
+
+    return null
+  } catch (error) {
+    console.error('Error getting latest commit:', error)
+    return null
+  }
+}
 
 export const onGitHubConnect = async (
   accessToken: string,
diff --git a/src/app/(main)/(pages)/connections/_actions/slack-connection.tsx b/src/app/(main)/(pages)/connections/_actions/slack-connection.tsx
index 6f979e3..c07e7fb 100644
--- a/src/app/(main)/(pages)/connections/_actions/slack-connection.tsx
+++ b/src/app/(main)/(pages)/connections/_actions/slack-connection.tsx
@@ -19,7 +19,7 @@ export const onSlackConnect = async (
 
   const slackConnection = await db.slack.findFirst({
     where: { slackAccessToken: slack_access_token },
-    include: { connections: true },
+    include: { Connections: true },
   })
 
   if (!slackConnection) {
diff --git a/src/app/(main)/(pages)/connections/page.tsx b/src/app/(main)/(pages)/connections/page.tsx
index 36a5200..eff58ac 100644
--- a/src/app/(main)/(pages)/connections/page.tsx
+++ b/src/app/(main)/(pages)/connections/page.tsx
@@ -2,12 +2,11 @@ import { CONNECTIONS } from '@/lib/constant'
 import React from 'react'
 import ConnectionCard from './_components/connection-card'
 import GoogleDriveListenerCard from './_components/google-drive-listener-card'
-import GitHubConnectionCard from './_components/github-connection-card'
+import GitHubListenerCard from './_components/github-listener-card'
 import { currentUser } from '@clerk/nextjs/server'
 import { onDiscordConnect } from './_actions/discord-connection'
 import { onNotionConnect } from './_actions/notion-connection'
 import { onSlackConnect } from './_actions/slack-connection'
-import { onGitHubConnect, getGitHubConnection } from './_actions/github-connection'
 import { getUserData } from './_actions/get-user'
 
 type Props = {
@@ -34,13 +33,6 @@ const Connections = async (props: Props) => {
     bot_user_id,
     team_id,
     team_name,
-    // GitHub OAuth params
-    github_access_token,
-    github_refresh_token,
-    github_login,
-    github_avatar_url,
-    github_name,
-    github_repositories,
   } = props.searchParams ?? {
     webhook_id: '',
     webhook_name: '',
@@ -60,12 +52,6 @@ const Connections = async (props: Props) => {
     bot_user_id: '',
     team_id: '',
     team_name: '',
-    github_access_token: '',
-    github_refresh_token: '',
-    github_login: '',
-    github_avatar_url: '',
-    github_name: '',
-    github_repositories: '',
   }
 
   const user = await currentUser()
@@ -115,31 +101,29 @@ const Connections = async (props: Props) => {
         )
       }
 
-      if (github_access_token && github_login) {
-        console.log('Connecting GitHub...')
-        const repositories = github_repositories 
-          ? JSON.parse(decodeURIComponent(github_repositories))
-          : []
-        
-        await onGitHubConnect(
-          github_access_token,
-          github_refresh_token || '',
-          github_login,
-          github_avatar_url || '',
-          github_name || '',
-          repositories,
-          user.id
-        )
-      }
-
       console.log('Getting user data...')
-      const user_info = await getUserData(user.id)
+      type UserInfo = {
+        name: string | null;
+        id: number;
+        clerkId: string;
+        email: string;
+        profileImage: string | null;
+        tier: string | null;
+        credits: string | null;
+        createdAt: Date;
+        updatedAt: Date;
+        localGoogleId: string | null;
+        googleResourceId: string | null;
+        Connections?: { type: string }[];
+      };
+
+      const user_info = await getUserData(user.id) as UserInfo;
       console.log('User info:', user_info)
 
       const connections: any = {}
 
       //get user info with all connections
-      user_info?.connections?.forEach((connection) => {
+      user_info?.Connections?.forEach((connection: { type: string }) => {
         connections[connection.type] = true
       })
 
@@ -154,10 +138,6 @@ const Connections = async (props: Props) => {
   }
 
   const connections = await onUserConnections()
-  
-  // Get GitHub connection data
-  const githubConnection = await getGitHubConnection(user.id)
-  const isGitHubConnected = !!githubConnection
 
   return (
     <div className="relative flex flex-col gap-4">
@@ -172,16 +152,8 @@ const Connections = async (props: Props) => {
           {/* Google Drive Listener Activation Card */}
           <GoogleDriveListenerCard />
           
-          {/* GitHub Connection Card */}
-          <GitHubConnectionCard 
-            connected={isGitHubConnected}
-            githubData={githubConnection ? {
-              login: githubConnection.login,
-              avatarUrl: githubConnection.avatarUrl || undefined,
-              name: githubConnection.name || undefined,
-              repositories: githubConnection.repositories || []
-            } : null}
-          />
+          {/* GitHub Listener Activation Card */}
+          <GitHubListenerCard />
           
           {CONNECTIONS.map((connection) => (
             <ConnectionCard
diff --git a/src/app/(main)/(pages)/dashboard/page.tsx b/src/app/(main)/(pages)/dashboard/page.tsx
index 084d1b2..2670a70 100644
--- a/src/app/(main)/(pages)/dashboard/page.tsx
+++ b/src/app/(main)/(pages)/dashboard/page.tsx
@@ -1,6 +1,7 @@
 import React from 'react'
 import { currentUser } from '@clerk/nextjs/server'
 import { db } from '@/lib/db'
+import { safeQuery } from '@/lib/db-helpers'
 import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card'
 import { Workflow, Zap, Link2, CreditCard } from 'lucide-react'
 import Link from 'next/link'
@@ -11,26 +12,61 @@ const DashboardPage = async () => {
   const user = await currentUser()
   if (!user) return null
 
-  // Fetch user data with all related information
-  const userData = await db.user.findUnique({
-    where: { clerkId: user.id },
-    include: {
-      workflows: true, // Get all workflows for accurate count
-      connections: true,
-    },
-  })
+  let userData: any = null
+  let recentWorkflows: any[] = []
+  
+  try {
+    // Fetch user data with all related information in a single query - with retry logic
+    userData = await safeQuery(
+      () => db.user.findUnique({
+        where: { clerkId: user.id },
+        include: {
+          Workflows: {
+            orderBy: { updatedAt: 'desc' },
+            take: 2, // Get only the 2 most recent for display
+          },
+          Connections: true,
+        },
+      })
+    )
 
-  // Get recent 2 workflows for display
-  const recentWorkflows = await db.workflows.findMany({
-    where: { userId: user.id },
-    orderBy: { updatedAt: 'desc' },
-    take: 2,
-  })
+    if (userData?.Workflows) {
+      recentWorkflows = userData.Workflows
+    }
+  } catch (error) {
+    console.error('Dashboard: Error fetching user data:', error)
+    // Return a basic dashboard with error state
+    return (
+      <div className="flex flex-col gap-4 relative">
+        <h1 className="text-4xl sticky top-0 z-[10] p-6 bg-background/50 backdrop-blur-lg flex items-center border-b justify-between">
+          Dashboard
+        </h1>
+        <div className="p-6">
+          <Card className="border-destructive">
+            <CardHeader>
+              <CardTitle>Unable to load dashboard</CardTitle>
+              <CardDescription>
+                Please refresh the page. If the issue persists, check your database connection.
+              </CardDescription>
+            </CardHeader>
+          </Card>
+        </div>
+      </div>
+    )
+  }
 
-  // Calculate stats
-  const totalWorkflows = userData?.workflows.length || 0
-  const publishedWorkflows = userData?.workflows.filter(w => w.publish).length || 0
-  const totalConnections = userData?.connections.length || 0
+  // Get total workflows count from database for accuracy - with retry
+  const totalWorkflows = await safeQuery(
+    () => db.workflows.count({ where: { userId: user.id } }),
+    0
+  )
+  
+  const publishedWorkflows = await safeQuery(
+    () => db.workflows.count({ where: { userId: user.id, publish: true } }),
+    0
+  )
+  
+  const totalConnections = userData?.Connections?.length || 0
   const credits = userData?.credits || '0'
   const tier = userData?.tier || 'Free'
   
diff --git a/src/app/(main)/(pages)/workflows/_actions/workflow-connections.tsx b/src/app/(main)/(pages)/workflows/_actions/workflow-connections.tsx
index 146b74a..84a9731 100644
--- a/src/app/(main)/(pages)/workflows/_actions/workflow-connections.tsx
+++ b/src/app/(main)/(pages)/workflows/_actions/workflow-connections.tsx
@@ -41,7 +41,9 @@ export const onCreateNodeTemplate = async (
   workflowId: string,
   channels?: Option[],
   accessToken?: string,
-  notionDbId?: string
+  notionDbId?: string,
+  githubRepo?: string,
+  githubEvents?: string[]
 ) => {
   if (type === 'Discord') {
     const response = await db.workflows.update({
@@ -133,6 +135,31 @@ export const onCreateNodeTemplate = async (
 
     if (response) return 'Notion template saved'
   }
+
+  if (type === 'GitHub') {
+    const user = await currentUser()
+    if (!user) return 'User not authenticated'
+
+    // Get GitHub connection for access token
+    const githubConnection = await db.gitHub.findFirst({
+      where: { userId: user.id },
+    })
+
+    if (!githubConnection) return 'GitHub not connected'
+
+    const response = await db.workflows.update({
+      where: {
+        id: workflowId,
+      },
+      data: {
+        githubRepo: githubRepo,
+        githubEvents: githubEvents || [],
+        githubAccessToken: githubConnection.accessToken,
+      },
+    })
+
+    if (response) return 'GitHub trigger configured'
+  }
 }
 
 export const onGetWorkflows = async () => {
diff --git a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/content-based-on-title.tsx b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/content-based-on-title.tsx
index e03bbdf..3bdafc3 100644
--- a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/content-based-on-title.tsx
+++ b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/content-based-on-title.tsx
@@ -15,10 +15,13 @@ import { onContentChange } from '@/lib/editor-utils'
 import GoogleFileDetails from './google-file-details'
 import GoogleDriveFiles from './google-drive-files'
 import GitHubNodeDetails from './github-node-details'
+import GitHubWorkflowConfig from './github-workflow-config'
+import GitHubCommitDetails from './github-commit-details'
 import ActionButton from './action-button'
 import { getFileMetaData } from '@/app/(main)/(pages)/connections/_actions/google-connection'
 import axios from 'axios'
 import { toast } from 'sonner'
+import { GitHubCommit } from '@/store'
 
 export interface Option {
   value: string
@@ -40,6 +43,8 @@ type Props = {
   setFile: (file: any) => void
   selectedSlackChannels: Option[]
   setSelectedSlackChannels: (value: Option[]) => void
+  githubCommit: GitHubCommit | null
+  setGithubCommit: (commit: GitHubCommit | null) => void
 }
 
 const ContentBasedOnTitle = ({
@@ -49,6 +54,11 @@ const ContentBasedOnTitle = ({
   setFile,
   selectedSlackChannels,
   setSelectedSlackChannels,
+  githubCommit,
+  setGithubCommit,
+}: Props) => {
+  selectedSlackChannels,
+  setSelectedSlackChannels,
 }: Props) => {
   const { selectedNode } = newState.editor
   const title = selectedNode.data.title
@@ -96,10 +106,17 @@ const ContentBasedOnTitle = ({
   if (title === 'GitHub') {
     return (
       <AccordionContent className="min-w-0 w-full p-0">
-        <GitHubNodeDetails
-          nodeConnection={nodeConnection}
-          title={title}
-        />
+        <Card className="min-w-0 w-full">
+          <CardHeader>
+            <CardTitle>GitHub Trigger Configuration</CardTitle>
+            <CardDescription>
+              Configure which repository and events trigger this workflow
+            </CardDescription>
+          </CardHeader>
+          <CardContent>
+            <GitHubWorkflowConfig />
+          </CardContent>
+        </Card>
       </AccordionContent>
     )
   }
@@ -139,6 +156,19 @@ const ContentBasedOnTitle = ({
               </CardContent>
             </Card>
           )}
+          {githubCommit && title !== 'GitHub' && (
+            <Card className="w-full min-w-0">
+              <CardContent className="px-2 py-3 min-w-0 w-full">
+                <div className="flex flex-col gap-4 min-w-0 w-full">
+                  <GitHubCommitDetails
+                    nodeConnection={nodeConnection}
+                    title={title}
+                    commit={githubCommit}
+                  />
+                </div>
+              </CardContent>
+            </Card>
+          )}
           {title === 'Google Drive' && <GoogleDriveFiles />}
           <ActionButton
             currentService={title}
diff --git a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-card-icon-hepler.tsx b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-card-icon-hepler.tsx
index 7545dc3..a8a7ad6 100644
--- a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-card-icon-hepler.tsx
+++ b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-card-icon-hepler.tsx
@@ -5,6 +5,7 @@ import {
   CircuitBoard,
   Database,
   GitBranch,
+  Github,
   HardDrive,
   Mail,
   MousePointerClickIcon,
@@ -55,6 +56,13 @@ const EditorCanvasIconHelper = ({ type }: Props) => {
           size={30}
         />
       )
+    case 'GitHub':
+      return (
+        <Github
+          className="flex-shrink-0"
+          size={30}
+        />
+      )
     case 'Notion':
       return (
         <Database
diff --git a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-card-single.tsx b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-card-single.tsx
index df623f5..eceb926 100644
--- a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-card-single.tsx
+++ b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-card-single.tsx
@@ -25,7 +25,7 @@ const EditorCanvasCardSingle = ({ data }: { data: EditorCanvasCardType }) => {
 
   return (
     <>
-      {data.type !== 'Trigger' && data.type !== 'Google Drive' && (
+      {data.type !== 'Trigger' && data.type !== 'Google Drive' && data.type !== 'GitHub' && (
         <CustomHandle
           type="target"
           position={Position.Top}
diff --git a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-sidebar.tsx b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-sidebar.tsx
index d438de1..c6d3158 100644
--- a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-sidebar.tsx
+++ b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas-sidebar.tsx
@@ -28,6 +28,7 @@ import {
 import RenderConnectionAccordion from './render-connection-accordion'
 import RenderOutputAccordion from './render-output-accordian'
 import { useFuzzieStore } from '@/store'
+import { getCurrentUserGitHubConnection, getLatestCommitFromRepo } from '@/app/(main)/(pages)/connections/_actions/github-connection'
 
 type Props = {
   nodes: EditorNodeType[]
@@ -36,7 +37,7 @@ type Props = {
 const EditorCanvasSidebar = ({ nodes }: Props) => {
   const { state } = useEditor()
   const { nodeConnection } = useNodeConnections()
-  const { googleFile, setSlackChannels } = useFuzzieStore()
+  const { googleFile, setSlackChannels, setGithubCommit } = useFuzzieStore()
   
   // Only fetch connections when the selected node changes
   useEffect(() => {
@@ -45,6 +46,33 @@ const EditorCanvasSidebar = ({ nodes }: Props) => {
     }
   }, [state.editor.selectedNode.data.title, googleFile])
 
+  // Fetch GitHub connection and latest commit when GitHub node is selected
+  useEffect(() => {
+    const fetchGitHubConnection = async () => {
+      if (state?.editor?.selectedNode?.data?.title === 'GitHub' && !nodeConnection.githubNode.accessToken) {
+        const connection = await getCurrentUserGitHubConnection()
+        if (connection) {
+          nodeConnection.setGitHubNode({
+            accessToken: connection.accessToken,
+            login: connection.login,
+            selectedRepo: connection.selectedRepo || '',
+            selectedEvents: connection.selectedEvents || [],
+            repositories: connection.repositories || [],
+          })
+          
+          // Fetch latest commit if a repo is selected
+          if (connection.selectedRepo) {
+            const latestCommit = await getLatestCommitFromRepo(connection.selectedRepo)
+            if (latestCommit) {
+              setGithubCommit(latestCommit)
+            }
+          }
+        }
+      }
+    }
+    fetchGitHubConnection()
+  }, [state.editor.selectedNode.data.title])
+
   // Only fetch Slack channels once when token is available
   useEffect(() => {
     if (nodeConnection.slackNode.slackAccessToken) {
diff --git a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas.tsx b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas.tsx
index 67f0d24..a3cb5e2 100644
--- a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas.tsx
+++ b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/editor-canvas.tsx
@@ -207,6 +207,7 @@ const EditorCanvas = (props: Props) => {
       AI: EditorCanvasCardSingle,
       Slack: EditorCanvasCardSingle,
       'Google Drive': EditorCanvasCardSingle,
+      'GitHub': EditorCanvasCardSingle,
       Notion: EditorCanvasCardSingle,
       Discord: EditorCanvasCardSingle,
       'Custom Webhook': EditorCanvasCardSingle,
diff --git a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/flow-instance.tsx b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/flow-instance.tsx
index 77270e0..5fba5ae 100644
--- a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/flow-instance.tsx
+++ b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/flow-instance.tsx
@@ -42,7 +42,7 @@ const FlowInstance = ({ children, edges, nodes }: Props) => {
     const visitedNodes = new Set<string>()
     
     // Find the trigger node (starting point)
-    const triggerNode = nodes.find((node) => node.type === 'Trigger' || node.type === 'Google Drive')
+    const triggerNode = nodes.find((node) => node.type === 'Trigger' || node.type === 'Google Drive' || node.type === 'GitHub')
     
     if (!triggerNode) {
       setIsFlow([])
@@ -63,8 +63,8 @@ const FlowInstance = ({ children, edges, nodes }: Props) => {
       outgoingEdges.forEach((edge) => {
         const targetNode = nodes.find((node) => node.id === edge.target)
         if (targetNode && !visitedNodes.has(targetNode.id)) {
-          // Skip Trigger and Google Drive from flow path (they're starting points)
-          if (targetNode.type !== 'Trigger' && targetNode.type !== 'Google Drive') {
+          // Skip Trigger, Google Drive, and GitHub from flow path (they're starting points)
+          if (targetNode.type !== 'Trigger' && targetNode.type !== 'Google Drive' && targetNode.type !== 'GitHub') {
             flows.push(targetNode.type)
           }
           visitedNodes.add(targetNode.id)
diff --git a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/github-node-details.tsx b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/github-node-details.tsx
index c39e9f3..1a824a7 100644
--- a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/github-node-details.tsx
+++ b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/github-node-details.tsx
@@ -8,6 +8,7 @@ import { Checkbox } from '@/components/ui/checkbox'
 import { useNodeConnections } from '@/providers/connections-provider'
 import { getGitHubConnection } from '@/app/(main)/(pages)/connections/_actions/github-connection'
 import { toast } from 'sonner'
+import { useUser } from '@clerk/nextjs'
 import {
   Select,
   SelectContent,
@@ -31,6 +32,7 @@ const GITHUB_EVENTS = [
 ]
 
 const GitHubNodeDetails = ({ nodeConnection, title }: Props) => {
+  const { user } = useUser()
   const { setNodeConnection } = useNodeConnections()
   const [selectedRepo, setSelectedRepo] = useState<string>('')
   const [selectedEvents, setSelectedEvents] = useState<string[]>(['push'])
@@ -39,10 +41,14 @@ const GitHubNodeDetails = ({ nodeConnection, title }: Props) => {
 
   useEffect(() => {
     const loadGitHubData = async () => {
+      if (!user) {
+        toast.error('User not authenticated')
+        setLoading(false)
+        return
+      }
+
       try {
-        // In production, get actual user ID from Clerk
-        const userId = 'current-user-id' // This should come from Clerk
-        const connection = await getGitHubConnection(userId)
+        const connection = await getGitHubConnection(user.id)
         
         if (connection) {
           setRepositories(connection.repositories || [])
@@ -64,7 +70,7 @@ const GitHubNodeDetails = ({ nodeConnection, title }: Props) => {
     }
 
     loadGitHubData()
-  }, [])
+  }, [user])
 
   const handleRepoChange = (repo: string) => {
     setSelectedRepo(repo)
diff --git a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/render-output-accordian.tsx b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/render-output-accordian.tsx
index 7180808..e570b08 100644
--- a/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/render-output-accordian.tsx
+++ b/src/app/(main)/(pages)/workflows/editor/[editorId]/_components/render-output-accordian.tsx
@@ -15,6 +15,8 @@ const RenderOutputAccordion = ({ state, nodeConnection }: Props) => {
     setGoogleFile,
     selectedSlackChannels,
     setSelectedSlackChannels,
+    githubCommit,
+    setGithubCommit,
   } = useFuzzieStore()
   return (
     <ContentBasedOnTitle
@@ -24,6 +26,8 @@ const RenderOutputAccordion = ({ state, nodeConnection }: Props) => {
       setFile={setGoogleFile}
       selectedSlackChannels={selectedSlackChannels}
       setSelectedSlackChannels={setSelectedSlackChannels}
+      githubCommit={githubCommit}
+      setGithubCommit={setGithubCommit}
     />
   )
 }
diff --git a/src/app/api/auth/callback/github/route.ts b/src/app/api/auth/callback/github/route.ts
index 71a6f26..9f846b8 100644
--- a/src/app/api/auth/callback/github/route.ts
+++ b/src/app/api/auth/callback/github/route.ts
@@ -24,7 +24,7 @@ export async function GET(req: NextRequest) {
     const tokenResponse = await axios.post(
       'https://github.com/login/oauth/access_token',
       {
-        client_id: process.env.GITHUB_CLIENT_ID!,
+        client_id: process.env.NEXT_PUBLIC_GITHUB_CLIENT_ID!,
         client_secret: process.env.GITHUB_CLIENT_SECRET!,
         code,
         redirect_uri: `${process.env.NEXT_PUBLIC_URL}/api/auth/callback/github`,
diff --git a/src/app/api/github/webhook/route.ts b/src/app/api/github/webhook/route.ts
index 2ae58bb..8e0e064 100644
--- a/src/app/api/github/webhook/route.ts
+++ b/src/app/api/github/webhook/route.ts
@@ -41,6 +41,44 @@ export async function POST(req: NextRequest) {
     console.log(`📦 Repository: ${repoFullName}`)
     console.log(`📋 Event: ${event}`)
 
+    // Extract commit data for template variables
+    let commitData: any = null
+    if (event === 'push' && payload.head_commit) {
+      commitData = {
+        message: payload.head_commit.message,
+        sha: payload.head_commit.id,
+        author: payload.head_commit.author?.name || 'Unknown',
+        authorEmail: payload.head_commit.author?.email || '',
+        timestamp: payload.head_commit.timestamp,
+        url: payload.head_commit.url,
+        branch: payload.ref?.replace('refs/heads/', '') || 'main',
+        repository: repoFullName,
+      }
+    } else if (event === 'pull_request' && payload.pull_request) {
+      commitData = {
+        message: payload.pull_request.title,
+        sha: payload.pull_request.head?.sha || '',
+        author: payload.pull_request.user?.login || 'Unknown',
+        authorEmail: '',
+        timestamp: payload.pull_request.created_at,
+        url: payload.pull_request.html_url,
+        branch: payload.pull_request.head?.ref || '',
+        repository: repoFullName,
+      }
+    } else {
+      // For other events, create minimal commit data
+      commitData = {
+        message: `${event} event`,
+        sha: '',
+        author: payload.sender?.login || 'Unknown',
+        authorEmail: '',
+        timestamp: new Date().toISOString(),
+        url: payload.repository?.html_url || '',
+        branch: '',
+        repository: repoFullName,
+      }
+    }
+
     // Find GitHub connection with this repo
     const githubConnection = await db.gitHub.findFirst({
       where: {
@@ -61,15 +99,23 @@ export async function POST(req: NextRequest) {
       return NextResponse.json({ message: 'Connection not found' }, { status: 404 })
     }
 
-    // Verify signature
-    if (githubConnection.webhookSecret) {
-      const isValid = verifySignature(rawBody, signature, githubConnection.webhookSecret)
-      if (!isValid) {
-        console.error('❌ Invalid webhook signature')
-        return NextResponse.json({ message: 'Invalid signature' }, { status: 401 })
-      }
+    // Verify signature - MANDATORY for security
+    if (!githubConnection.webhookSecret) {
+      console.error('❌ No webhook secret configured for this connection')
+      return NextResponse.json(
+        { message: 'Webhook not properly configured' },
+        { status: 500 }
+      )
     }
 
+    const isValid = verifySignature(rawBody, signature, githubConnection.webhookSecret)
+    if (!isValid) {
+      console.error('❌ Invalid webhook signature - possible attack or misconfiguration')
+      return NextResponse.json({ message: 'Invalid signature' }, { status: 401 })
+    }
+
+    console.log('✅ Webhook signature verified successfully')
+
     // Check if this event is enabled for this repo
     if (
       githubConnection.selectedRepo === repoFullName &&
@@ -158,14 +204,24 @@ export async function POST(req: NextRequest) {
                 if (discordMessage && flow.discordTemplate) {
                   // Replace GitHub variables in template
                   let message = flow.discordTemplate
+                  
+                  // Replace GitHub template variables
+                  if (commitData) {
+                    message = message
+                      .replace(/\{\{commit_message\}\}/g, commitData.message)
+                      .replace(/\{\{commit_sha\}\}/g, commitData.sha.substring(0, 7))
+                      .replace(/\{\{author\}\}/g, commitData.author)
+                      .replace(/\{\{author_email\}\}/g, commitData.authorEmail)
+                      .replace(/\{\{repository\}\}/g, commitData.repository)
+                      .replace(/\{\{branch\}\}/g, commitData.branch)
+                      .replace(/\{\{timestamp\}\}/g, new Date(commitData.timestamp).toLocaleString())
+                  }
+                  
                   message = message.replace(/\{\{event\}\}/g, event)
                   message = message.replace(/\{\{repo\}\}/g, repoFullName)
                   if (payload.sender) {
                     message = message.replace(/\{\{sender\}\}/g, payload.sender.login)
                   }
-                  if (payload.commits && payload.commits.length > 0) {
-                    message = message.replace(/\{\{commit_message\}\}/g, payload.commits[0].message)
-                  }
 
                   await postContentToWebHook(message, discordMessage.url)
                   console.log(`  ✓ Discord message sent`)
@@ -179,6 +235,7 @@ export async function POST(req: NextRequest) {
               if (action === 'Slack') {
                 if (
                   flow.slackAccessToken &&
+                  flow.slackChannels &&
                   flow.slackChannels.length > 0 &&
                   flow.slackTemplate
                 ) {
@@ -187,16 +244,25 @@ export async function POST(req: NextRequest) {
                     value: channel,
                   }))
 
-                  // Replace GitHub variables in template
                   let message = flow.slackTemplate
+                  
+                  // Replace GitHub template variables
+                  if (commitData) {
+                    message = message
+                      .replace(/\{\{commit_message\}\}/g, commitData.message)
+                      .replace(/\{\{commit_sha\}\}/g, commitData.sha.substring(0, 7))
+                      .replace(/\{\{author\}\}/g, commitData.author)
+                      .replace(/\{\{author_email\}\}/g, commitData.authorEmail)
+                      .replace(/\{\{repository\}\}/g, commitData.repository)
+                      .replace(/\{\{branch\}\}/g, commitData.branch)
+                      .replace(/\{\{timestamp\}\}/g, new Date(commitData.timestamp).toLocaleString())
+                  }
+                  
                   message = message.replace(/\{\{event\}\}/g, event)
                   message = message.replace(/\{\{repo\}\}/g, repoFullName)
                   if (payload.sender) {
                     message = message.replace(/\{\{sender\}\}/g, payload.sender.login)
                   }
-                  if (payload.commits && payload.commits.length > 0) {
-                    message = message.replace(/\{\{commit_message\}\}/g, payload.commits[0].message)
-                  }
 
                   await postMessageToSlack(flow.slackAccessToken, channels, message)
                   console.log(`  ✓ Slack message sent to ${flow.slackChannels.length} channel(s)`)
diff --git a/src/components/infobar/index.tsx b/src/components/infobar/index.tsx
index 8ae2f91..1e57309 100644
--- a/src/components/infobar/index.tsx
+++ b/src/components/infobar/index.tsx
@@ -1,5 +1,5 @@
 'use client'
-import React, { useEffect } from 'react'
+import React, { useEffect, useCallback } from 'react'
 import { ModeToggle } from '../global/mode-toggle'
 import { Book, Headphones, Search } from 'lucide-react'
 //import Templates from '../icons/cloud_download'
@@ -20,17 +20,17 @@ type Props = {}
 const InfoBar = (props: Props) => {
   const { credits, tier, setCredits, setTier } = useBilling()
 
-  const onGetPayment = async () => {
+  const onGetPayment = useCallback(async () => {
     const response = await onPaymentDetails()
     if (response) {
       setTier(response.tier!)
       setCredits(response.credits!)
     }
-  }
+  }, [setCredits, setTier])
 
   useEffect(() => {
     onGetPayment()
-  }, [])
+  }, [onGetPayment])
 
   return (
     <div className="flex flex-row justify-end gap-6 items-center px-4 py-4 w-full dark:bg-black ">
diff --git a/src/lib/db.ts b/src/lib/db.ts
index 2de203d..12ea553 100644
--- a/src/lib/db.ts
+++ b/src/lib/db.ts
@@ -1,9 +1,24 @@
 import { PrismaClient } from '@prisma/client'
 
-declare global {
-  var prisma: PrismaClient | undefined
+const prismaClientSingleton = () => {
+  return new PrismaClient({
+    log: process.env.NODE_ENV === 'development' ? ['error', 'warn'] : ['error'],
+    datasources: {
+      db: {
+        url: process.env.DATABASE_URL,
+      },
+    },
+  })
 }
 
-export const db = globalThis.prisma || new PrismaClient()
+type PrismaClientSingleton = ReturnType<typeof prismaClientSingleton>
 
-if (process.env.NODE_ENV !== 'production') globalThis.prisma = db
\ No newline at end of file
+const globalForPrisma = globalThis as unknown as {
+  prisma: PrismaClientSingleton | undefined
+}
+
+export const db = globalForPrisma.prisma ?? prismaClientSingleton()
+
+if (process.env.NODE_ENV !== 'production') {
+  globalForPrisma.prisma = db
+}
\ No newline at end of file
diff --git a/src/lib/types.ts b/src/lib/types.ts
index d4b6d25..a01bf17 100644
--- a/src/lib/types.ts
+++ b/src/lib/types.ts
@@ -23,6 +23,25 @@ export type Connection = {
   slackSpecial?: boolean
 }
 
+/**
+ * Google Drive File metadata type
+ * Replaces 'any' type for better type safety
+ */
+export interface GoogleFileMetadata {
+  id: string
+  name: string
+  mimeType: string
+  webViewLink?: string
+  webContentLink?: string
+  iconLink?: string
+  thumbnailLink?: string
+  createdTime?: string
+  modifiedTime?: string
+  size?: string
+  driveId?: string
+  kind?: string
+}
+
 export type EditorCanvasTypes =
   | 'Email'
   | 'Condition'
@@ -42,7 +61,7 @@ export type EditorCanvasCardType = {
   description: string
   completed: boolean
   current: boolean
-  metadata: any
+  metadata: Record<string, unknown> // Better than 'any'
   type: EditorCanvasTypes
 }
 
diff --git a/src/store.tsx b/src/store.tsx
index d9a8d9a..9bccfbe 100644
--- a/src/store.tsx
+++ b/src/store.tsx
@@ -1,4 +1,5 @@
 import { create } from 'zustand'
+import type { GoogleFileMetadata } from '@/lib/types'
 
 export interface Option {
   value: string
@@ -10,23 +11,35 @@ export interface Option {
   [key: string]: string | boolean | undefined
 }
 
+export interface GitHubCommit {
+  sha: string
+  message: string
+  author: string
+  authorEmail: string
+  timestamp: string
+  repository: string
+  url: string
+  branch?: string
+}
+
 type FuzzieStore = {
-  googleFile: any
-  setGoogleFile: (googleFile: any) => void
+  googleFile: GoogleFileMetadata | Record<string, never>
+  setGoogleFile: (googleFile: GoogleFileMetadata | Record<string, never>) => void
   slackChannels: Option[]
   setSlackChannels: (slackChannels: Option[]) => void
   selectedSlackChannels: Option[]
   setSelectedSlackChannels: (selectedSlackChannels: Option[]) => void
+  githubCommit: GitHubCommit | null
+  setGithubCommit: (githubCommit: GitHubCommit | null) => void
 }
 
 export const useFuzzieStore = create<FuzzieStore>()((set) => ({
   googleFile: {},
-  setGoogleFile: (googleFile: any) => set({ googleFile }),
+  setGoogleFile: (googleFile: GoogleFileMetadata | Record<string, never>) => set({ googleFile }),
   slackChannels: [],
   setSlackChannels: (slackChannels: Option[]) => set({ slackChannels }),
   selectedSlackChannels: [],
   setSelectedSlackChannels: (selectedSlackChannels: Option[]) =>
     set({ selectedSlackChannels }),
-}))
-
-
+  githubCommit: null,
+  setGithubCommit: (githubCommit: GitHubCommit | null) => set({ githubCommit }),
